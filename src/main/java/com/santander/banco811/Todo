 todo:
Trabalho do Módulo:
( x ) - CRUD  de conta
  ( X )  addConta() - CREATE
  ( X )  getConta() - READ
  ( X )  updateConta() - UPDATE
  ( X )  deleteConta() - DELETE

(  ) - Mapeamento da entidade de transação
    // Assistir aula do dia:

( x ) - Busca Paginada
    (  ) ACCOUNT
    (  ) USER
    (  ) TRANSACTION
    // Assistir aula do dia 4
    Pageable and PageRequest.

( x ) - Alguma busca por query
    // Assistir aula do dia:

(  ) - Pelo menos um teste de repository
    // Assistir aula do dia 4

